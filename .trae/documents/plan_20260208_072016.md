I am proceeding with the implementation of Filament Clusters to group the Purchase Orders as requested.

## Execution Steps:
1.  **Create Cluster**: `PurchaseOrders` (Already created).
2.  **Update PurchaseOrderResource (In)**:
    *   Assign to `PurchaseOrders` cluster.
    *   Remove `$navigationGroup` ('Sales').
    *   Rename navigation label to "Incoming (Sales)".
3.  **Update PurchaseOrderOutResource (Out)**:
    *   Assign to `PurchaseOrders` cluster.
    *   Remove `$navigationGroup` ('Procurement').
    *   Rename navigation label to "Outgoing (Procurement)".
4.  **Verify**: Ensure the sidebar shows a single "Purchase Orders" group with the two sub-items.

I will now apply these code changes.
